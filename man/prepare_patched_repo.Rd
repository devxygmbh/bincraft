% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patches.R
\name{prepare_patched_repo}
\alias{prepare_patched_repo}
\title{Prepare a local repo of patched binaries for the current build}
\usage{
prepare_patched_repo(
  patches_dir,
  platform,
  arch,
  r_minor,
  cache_dir = file.path("/mnt", "cache", "patched-binaries"),
  repo_dir = tempfile("patched_repo_")
)
}
\arguments{
\item{patches_dir}{Directory with \code{registry.json}, or \code{NULL}.}

\item{platform}{Build platform, e.g. \code{"ubuntu-2604"}.}

\item{arch}{Build arch, e.g. \code{"amd64"}.}

\item{r_minor}{R \code{"major.minor"} string, e.g. \code{"4.5"}.}

\item{cache_dir}{Persistent cache for patched binaries.}

\item{repo_dir}{Directory to assemble the local repo in.}
}
\value{
\code{repo_dir} if at least one patched binary was produced, else \code{NULL}.
}
\description{
For each registry entry matching the current platform, ensures a patched
binary is present in \code{repo_dir} (from \code{cache_dir} if available, else built
and then cached) and writes a \code{PACKAGES} index over them.
}
\keyword{internal}
