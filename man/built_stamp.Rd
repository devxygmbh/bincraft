% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package_index.R
\name{built_stamp}
\alias{built_stamp}
\title{Construct a \code{Built} field value for the current build environment}
\usage{
built_stamp(
  platform = R.version$platform,
  r_version = getRversion(),
  time = Sys.time()
)
}
\arguments{
\item{platform}{Platform triple, defaulting to \code{R.version$platform}.}

\item{r_version}{R version, defaulting to \code{\link[base:numeric_version]{getRversion()}}.}

\item{time}{Build time, defaulting to \code{\link[base:Sys.time]{Sys.time()}}.}
}
\description{
cranlike derives package metadata from the CRAN \emph{source} \code{DESCRIPTION}, which
never carries a \code{Built:} field (R stamps that only when it builds a binary).
Our S3 tarballs \emph{are} binaries, so we pass this stamp to
\code{\link[cranlike:update_PACKAGES]{cranlike::update_PACKAGES()}}/\code{\link[cranlike:add_PACKAGES]{cranlike::add_PACKAGES()}} to advertise them as
such. Binary-aware clients (e.g. \code{uvr}) key their binary-vs-source decision on
the \code{Built:} platform triple + R minor; without it they treat the repo as
source-only and compile everything, which needs system \code{-dev} libraries.
}
\details{
The triple comes from \code{R.version$platform} and the version from
\code{\link[base:numeric_version]{getRversion()}}, so this must run in the same build environment that produced
the binaries (the surrounding arch/codename logic already assumes this).
}
\keyword{internal}
