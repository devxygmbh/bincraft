% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abi_classify.R
\name{abi_classify}
\alias{abi_classify}
\title{Classify an R source package by its ABI-risk tier}
\usage{
abi_classify(path)
}
\arguments{
\item{path}{Path to an unpacked source-package directory (containing
\code{DESCRIPTION}) or to a \code{.tar.gz} of one. Tarballs are untarred to a
temporary directory.}
}
\value{
A list with elements:
\itemize{
\item \code{tier}: one of \code{"pure-r"}, \code{"safe-compiled"}, \code{"risky"}.
\item \code{reason}: short string explaining the classification.
\item \code{hits}: character vector of packages or symbols that triggered \code{"risky"};
empty otherwise.
}
}
\description{
Inspects an unpacked R source package (or a \code{.tar.gz} of one) and labels it
as one of:
}
\details{
\itemize{
\item \code{"pure-r"}: no compilation needed — build once, serve for every
R-version slot.
\item \code{"safe-compiled"}: compiles, but only touches the stable R C-API — build
once per major.minor.
\item \code{"risky"}: links to a known-volatile dependency (e.g. \code{Rcpp}, \code{rlang}) or
references internal R C-API symbols known to change across minor versions
— build per R minor version.
}

Decision rules (first match wins):
\enumerate{
\item \code{pure-r} if \code{DESCRIPTION} has \code{NeedsCompilation: no}, or there is no
\verb{src/}, or \verb{src/} contains no \code{.c}, \code{.cc}, \code{.cpp}, \code{.f}, \code{.f90} files.
\item \code{risky} if \code{LinkingTo} mentions any package in
\code{\link[=abi_risky_linking_deps]{abi_risky_linking_deps()}}.
\item \code{risky} if any file in \verb{src/} mentions a symbol in
\code{\link[=abi_volatile_symbols]{abi_volatile_symbols()}} (plain regex grep, no C parsing).
\item \code{safe-compiled} otherwise.
}
}
\seealso{
\code{\link[=abi_volatile_symbols]{abi_volatile_symbols()}}, \code{\link[=abi_risky_linking_deps]{abi_risky_linking_deps()}}.
}
