% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abi_classify.R
\name{needs_per_minor_recompile}
\alias{needs_per_minor_recompile}
\title{Does this package need to be recompiled per R minor version?}
\usage{
needs_per_minor_recompile(path)
}
\arguments{
\item{path}{Path to an unpacked source-package directory (containing
\code{DESCRIPTION}) or to a \code{.tar.gz} of one. Tarballs are untarred to a
temporary directory.}
}
\value{
A logical of length 1. Attributes:
\itemize{
\item \code{tier}: the \code{\link[=abi_classify]{abi_classify()}} tier (\code{"pure-r"}, \code{"safe-compiled"}, or
\code{"risky"}).
\item \code{reason}: short human-readable string.
\item \code{hits}: packages or symbols that triggered \code{"risky"}; empty otherwise.
}
}
\description{
Thin convenience wrapper around \code{\link[=abi_classify]{abi_classify()}} that returns a single
logical: \code{TRUE} if the package must be recompiled per R minor version
(tier \code{"risky"}), \code{FALSE} otherwise.
}
\details{
Note this is a strictly bincraft-internal "do I need to invoke the
compiler again per minor?" question — it is not the same as the
artifact-level "does one binary work across all R versions?" question
(which is TRUE only for \code{pure-r}). \code{safe-compiled} is FALSE here (one
compile is enough) but is still stored per-minor at the artifact level.

The full classification is attached as attributes (\code{tier}, \code{reason},
\code{hits}) so callers can inspect \emph{why} without a second call.
}
\examples{
\dontrun{
result <- needs_per_minor_recompile("path/to/pkg")
if (result) {
  message("Recompile per R minor; reason: ", attr(result, "reason"))
}
}
}
\seealso{
\code{\link[=abi_classify]{abi_classify()}} for the full structured result.
}
