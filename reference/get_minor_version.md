# Get R minor version string

Extracts the major.minor version string from the current R version. For
example, R 4.3.2 returns "4.3".

## Usage

``` r
get_minor_version()
```

## Value

Character string with major.minor version
