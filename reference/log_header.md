# Log a header message with cli formatting

Log a header message with cli formatting

## Usage

``` r
log_header(msg, ...)
```

## Arguments

- msg:

  The message to log

- ...:

  Additional arguments passed to cli functions
