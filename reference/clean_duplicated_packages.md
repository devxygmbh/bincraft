# Clean duplicated package files

Removes duplicate package files that may have been created due to build
errors or upload issues.

## Usage

``` r
clean_duplicated_packages(old_versions)
```

## Arguments

- old_versions:

  (character)  
  Character vector of old version file paths

## Value

Character vector with duplicates removed
