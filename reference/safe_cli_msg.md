# Safely escape curly braces for cli while preserving intentional formatting

Safely escape curly braces for cli while preserving intentional
formatting

## Usage

``` r
safe_cli_msg(msg)
```

## Arguments

- msg:

  The message to process

## Value

Message with unmatched braces escaped
