# Log an info message with cli formatting

Log an info message with cli formatting

## Usage

``` r
log_info(msg, ...)
```

## Arguments

- msg:

  The message to log

- ...:

  Additional arguments passed to cli functions
