# Packages whose `LinkingTo` presence marks a package as ABI-risky: they
# expose or wrap R internals that change across R minor versions, so anything
# linking to them must be rebuilt per R minor.
#
# Format: one package name per line. Blank lines and lines starting with `#`
# ignored.

rlang
Rcpp
cpp11
vctrs
